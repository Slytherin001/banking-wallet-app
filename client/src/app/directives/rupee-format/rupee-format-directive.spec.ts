import { RupeeFormatDirective } from './rupee-format-directive';

describe('RupeeFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new RupeeFormatDirective();
    expect(directive).toBeTruthy();
  });
});
